@page "/Company"
@using Pilot.BlazorClient.Interface
@using Pilot.BlazorClient.ViewModels
@inject ICompanyPageService PageService

<div class="container mt-5">
    <h2 class="text-center mb-4" style="color: #4B0082;">Компания</h2>

    @if (_companies.Any())
    {
        @foreach (var company in _companies)
        {
            <div class="card mb-4 shadow-lg" style="border-radius: 10px; background-color: #f8f9fa;">
                <div class="card-body">
                    <h3 class="card-title" style="color: #4B0082;">@company.Title</h3>

                    @if (!string.IsNullOrEmpty(company.Description))
                    {
                        <p class="card-text">@company.Description</p>
                    }

                    @if (company.LogoId != null)
                    {
                        <img src="@company.LogoId" alt="Company Logo" class="img-fluid" style="max-width: 150px;" />
                    }

                    <h5 class="mt-4" style="color: #4B0082;">Проекты</h5>
                    @if (company.Projects.Any())
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var project in company.Projects)
                            {
                                <li class="list-group-item">@project.Name</li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p>Нет проектов.</p>
                    }
                </div>
            </div>
        }
    }
    else
    {
        <p class="text-center">Нет доступных компаний.</p>
    }
</div>

@code {
    private ICollection<CompanyViewModel> _companies = new List<CompanyViewModel>();

    protected override async Task OnInitializedAsync()
    {
        _companies = await PageService.GetCompanyListAsync();
    }
}
